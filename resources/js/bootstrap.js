/*
 * Axios scaffold import removed (SPEC-015 FR-009, §12; ARCHITECTURE §19).
 *
 * The default Laravel scaffold imported axios to send API requests, but this
 * application has no API target and axios is not part of the declared
 * frontend stack. Alpine.js is the only runtime JavaScript (see app.js).
 */
